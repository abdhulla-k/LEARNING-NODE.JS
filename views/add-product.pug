extends layouts/main_layout.pug

block content
    main
        form.product-form(action="/product", method="POST")
            .form-control
                label(for="title") Title
                input(type="text", name="title", id="title")
            button.btn(type="submit") Add Product